#!/usr/bin/env node

// import or require lilmolly.js here and then export it so that it can be used in other files as an npm package

const lilmolly = require("./lilmolly.js");

module.exports = lilmolly;
